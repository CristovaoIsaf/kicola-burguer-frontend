# Em desenvolvimento, o proxy do Vite reencaminha /api/* para o backend
# Deixar vazio usa o proxy; ou definir URL completa para produção
# Em produção, definir a URL real do backend
VITE_API_URL=http://localhost:8080
